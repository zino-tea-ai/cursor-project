# Vitaflow Replica

从 Figma 设计稿一比一复刻的 Vitaflow 卡路里追踪 App 界面。

## 🎨 设计系统

设计令牌已从 Figma 提取并保存在 `src/styles/design-tokens.css` 中，包含：

- **颜色系统**: 主色、强调色、中性色、状态颜色
- **字体系统**: 字体家族、字号、字重、行高
- **间距系统**: 统一的间距变量
- **圆角系统**: 各种圆角尺寸
- **阴影系统**: 卡片阴影、导航阴影

## 📁 项目结构

```
vitaflow-replica/
├── src/
│   ├── components/          # React 组件
│   │   ├── StatusBar.tsx    # 状态栏
│   │   ├── Header.tsx       # 头部
│   │   ├── CalendarStrip.tsx# 日历条
│   │   ├── CalorieCard.tsx  # 卡路里卡片
│   │   ├── MacroCards.tsx   # 宏量营养素卡片
│   │   ├── MealTabs.tsx     # 餐食标签
│   │   ├── FoodList.tsx     # 食物列表
│   │   └── BottomNavigation.tsx # 底部导航
│   ├── styles/
│   │   └── design-tokens.css # 设计令牌
│   ├── App.tsx              # 主应用组件
│   ├── main.tsx             # 入口文件
│   └── index.css            # 全局样式
├── tests/
│   ├── design-consistency.spec.ts  # 设计一致性测试
│   └── visual-comparison.spec.ts   # 视觉对比测试
├── playwright.config.ts     # Playwright 配置
├── tailwind.config.js       # Tailwind 配置
└── package.json
```

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:5173 查看页面。

### 安装 Playwright 浏览器

```bash
npx playwright install
```

## 🧪 运行测试

### 运行所有测试

```bash
npm test
```

### 运行带 UI 的测试

```bash
npm run test:ui
```

### 更新截图基准

```bash
npm run test:update
```

## 📊 设计一致性测试

测试套件包含：

1. **视觉回归测试** - 组件和页面截图对比
2. **布局测试** - 验证尺寸、间距、对齐
3. **颜色测试** - 验证颜色值正确应用
4. **交互测试** - 验证按钮、Tab 等交互功能
5. **设计规范对比** - 与 Figma 规范进行精确对比

### 测试报告

运行测试后，报告将生成在 `playwright-report/` 目录中。

## 📐 Figma 设计规范

| 属性 | 值 |
|------|-----|
| 设备宽度 | 402px |
| 设备高度 | 874px |
| 状态栏高度 | 62px |
| 底部导航高度 | 98px |
| 页面边距 | 20px |
| 卡片圆角 | 16px |
| 主色 | #2DD4BF |
| 背景色 | #F5F5F5 |

## 🛠 技术栈

- **React 18** - UI 框架
- **TypeScript** - 类型安全
- **Vite** - 构建工具
- **Tailwind CSS** - 样式框架
- **Lucide React** - 图标库
- **Playwright** - E2E 测试

## 📝 开发注意事项

1. 所有组件都带有 `data-testid` 属性，方便测试定位
2. 设计令牌同时在 CSS 变量和 Tailwind 配置中定义
3. 颜色值与 Figma 设计保持完全一致
4. 布局尺寸严格按照 Figma 标注实现
